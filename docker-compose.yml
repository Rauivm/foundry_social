services:
  web:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
    environment:
      RAILS_ENV: development
      BUNDLE_PATH: /usr/local/bundle
    command: bash -lc "bundle exec rails db:setup && bundle exec rails server -b 0.0.0.0 -p 3000"
